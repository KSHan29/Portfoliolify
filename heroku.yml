build:
  docker:
    web: Portfoliolify/Dockerfile
    context: Portfoliolify

release:
  command:
    - python manage.py migrate

run:
  web: gunicorn backend.wsgi:application --bind 0.0.0.0:8000
